# ZenithCore

ZenithCore is a Spring Boot boilerplate (Java 21) demonstrating clean architecture: controller → service → repository.

Run with:
```
./gradlew bootRun
```

API endpoints: `/api/tasks` (CRUD).

License: MIT
# zenith-core
